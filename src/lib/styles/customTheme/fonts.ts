import type { DeepPartial, Theme } from '@chakra-ui/react';

const fonts: DeepPartial<Theme['fonts']> = {
  body: '"DM Sans", sans-serif',
  heading: '"BR Firma Bold", serif',
};

export default fonts;
